
Update v3:
- Script NEVER stops on Invoke-Sqlcmd failures
- Network / instance connection errors skip to next instance
- Explicit continue logic added
- Safe for unstable networks and large instance counts
